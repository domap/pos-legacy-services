1. Create Kubernetes Secret (or ExternalSecret) named in values `externalSecrets.databaseSecretName` with keys jdbc-url, username, password (names configurable in values.yaml).
2. Set image.repository and image.tag to your ECR digest or immutable tag from CI.
3. Set serviceAccount.annotations for EKS IRSA if the workload needs AWS APIs.
