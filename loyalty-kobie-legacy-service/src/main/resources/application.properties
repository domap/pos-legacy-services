server.port=8082
spring.application.name=loyalty-kobie-legacy

# Kobie endpoint (replace in real env — legacy pattern keeps default + file override)
kobie.api.baseUrl=http://localhost:9999/kobie-mock

logging.level.root=INFO
